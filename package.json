{
  "name": "opencode-morphllm",
  "version": "0.0.6",
  "author": "Vito Lin",
  "main": "dist/index.js",
  "description": "OpenCode plugin for MorphLLM",
  "scripts": {
    "build": "rm -rf dist && tsc -p tsconfig.json",
    "test": "bun test",
    "test:coverage": "bun test --coverage && bunx coverage-badges-cli --source coverage/coverage-summary.json",
    "format": "prettier --write .",
    "format:check": "prettier --check .",
    "prepare": "husky install"
  },
  "husky": {
    "hooks": {
      "post-commit": "bun run test:coverage && git add -A && git commit --amend --no-verify"
    }
  },
  "opencode": {
    "plugin": true
  },
  "lint-staged": {
    "*.{js,jsx,ts,tsx,json,css,md}": [
      "prettier --write"
    ]
  },
  "type": "module",
  "types": "dist/index.d.ts",
  "dependencies": {
    "@morphllm/morphsdk": "^0.2.102",
    "@opencode-ai/plugin": "1.1.36"
  },
  "devDependencies": {
    "bun-types": "^1.3.6",
    "coverage-badges-cli": "^2.2.0",
    "husky": "^8.0.3",
    "lint-staged": "^15.2.0",
    "prettier": "^3.2.5",
    "typescript": "^5.9.3"
  }
}
